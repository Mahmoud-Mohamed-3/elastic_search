<p style="color: green"><%= notice %></p>

<% content_for :title, "Resources" %>

<h1>Resources</h1>

<div id="resources">
  <% @order_items.each do |resource| %>
    <%= render resource %>
    <p>
      <%= link_to "Show this order_items", resource %>
    </p>
  <% end %>
</div>

<%= link_to "New order_items", new_resource_path %>
